version: "3.8"

services:
  linux-terminal:
    build: .
    ports:
      - "7681:7681"
      - "2222:22"
    environment:
      - PORT=7681
      - TERM=xterm-256color
      - SSH_PASSWORD=changeme123
    restart: unless-stopped
